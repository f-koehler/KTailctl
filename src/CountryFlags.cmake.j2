{% for flag in flags %}
set_source_files_properties(${CMAKE_CURRENT_LIST_DIR}/country-flags/{{ flag }}.svg PROPERTIES QT_RESOURCE_ALIAS
country-flag-{{ flag }}.svg)
{% endfor %}

set(KTAILCTL_FLAG_FILES
{% for flag in flags %}
${CMAKE_CURRENT_LIST_DIR}/country-flags/{{ flag }}.svg
{% endfor %}
)