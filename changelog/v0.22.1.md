# KTailctl v0.22.1

A patch release fixing several issues introduced in v0.22.0.

## Fixes

- Honor the "Start minimized" setting again on startup
- Remove the duplicate "Allow LAN access" toggle on the exit node page
- Fix tray submenus (exit nodes) sometimes not updating
- Fix a potential crash in the tray menu when there is no previously used exit node
