[project]
name = "ktailctl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["jinja2>=3.1.5", "pycountry>=24.6.1", "rich>=15.0.0"]

[tool.pyright]
venvPath = "."
venv = ".devenv/state/venv"
